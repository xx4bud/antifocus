import { SignUpForm } from "@/app/(auth)/signup/signup-form";

export default function SignUpPage() {
  return (
    <div className="container-wrapper flex flex-1 items-center justify-center">
      <SignUpForm />
    </div>
  );
}
